//! Workspace and document model with classification and invalidation.
//!
//! Declared empty in Task 1; implemented in Task 2.
//!
//! TODO(Task 2): snapshot-based `TrackedDocument` text tracking, workspace root
//! detection, `GradleFileKind` classification, and an `InvalidationScope` contract
//! mapping each change trigger to file-only reparse vs workspace semantic invalidation
//! vs sidecar refresh. No coupling to parser node types or sidecar logic.
