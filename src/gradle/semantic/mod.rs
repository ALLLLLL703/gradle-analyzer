//! Static semantic graph over the parsed syntax trees.
//!
//! Declared empty in Task 1; implemented in Task 7.
//!
//! TODO(Task 7): per-document `SemanticDocument` records with stable semantic IDs and
//! source ownership for includes, plugins, repositories, dependencies, tasks, imports,
//! version catalogs, and buildSrc symbols. Offline only; no sidecar dependency.
