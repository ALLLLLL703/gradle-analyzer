//! Shared tolerant syntax substrate (lexer, green/red tree, spans, typed errors).
//!
//! Declared empty in Task 1; implemented in Task 3 (`gradle-analyzer-lsp-v2` plan).
//!
//! TODO(Task 3): lossless lexer, `TextSpan`, generic `SyntaxKind`, immutable green nodes
//! plus a red-tree cursor, checkpoint-based construction, a typed `SyntaxErrorKind`, a
//! recoverable parser driver, and bounded opaque-node fallback. No Gradle/Groovy/Kotlin
//! semantics here.
